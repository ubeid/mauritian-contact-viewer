apply plugin: 'com.android.application'

android {
    signingConfigs {
        projectDefault {
            keyAlias 'MCM_Key'
            keyPassword 'adz@k.com'
            storeFile file('C:/Users/Ubeidullah/mcm_key.jks')
            storePassword 'adz@k.com'
        }
    }
    compileSdkVersion 22
    buildToolsVersion "21.1.2"
    defaultConfig {
        applicationId "adz.mauritius.subscribers.contact.viewer"
        minSdkVersion 11
        targetSdkVersion 22
        versionCode 2
        versionName "1.1.0"
    }
    buildTypes {
        release {
            zipAlignEnabled true
            signingConfig signingConfigs.projectDefault
        }
        debug {
            zipAlignEnabled true
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:22.2.1'
}
